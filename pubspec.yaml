name: order_management_demo
description: Event-sourced restaurant order management demo (fmodel, Jaspr, SQLite)
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  fmodel: ^0.1.0-dev.1
  jaspr: ^0.23.1
  jaspr_router: ^0.8.2
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^3.3.4
  ulid: ^2.0.1
  universal_web: ^1.1.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.8.0
  jaspr_builder: ^0.23.1
  lints: ^5.0.0
  test: ^1.26.0

jaspr:
  mode: server
